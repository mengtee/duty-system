{
    "build": {
      "builder": "nixpacks"
    },
    "deploy": {
      "startCommand": "pip install -r backend/requirements.txt && cd backend && uvicorn app:app --host 0.0.0.0 --port $PORT"
    }
  }